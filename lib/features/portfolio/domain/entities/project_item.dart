class ProjectItem {
  const ProjectItem({
    required this.title,
    required this.description,
    required this.isPlanned,
  });

  final String title;
  final String description;
  final bool isPlanned;
}
